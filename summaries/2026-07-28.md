# llama.cpp daily summary -- 2026-07-28

Active day with 13 commits: two new model additions (MiMo-V2.5 audio and Nanbeige4.2), several backend and GPU build fixes, a notable server streaming fix, and a new `mlock`-without-mmap CLI option. The ggml backend offloading logic was also reworked, with follow-on DeepSeek v4 graph fixes.

## New Models

- **mtmd**: add MiMo-V2.5 audio input support via RVQ-based model; includes GGUF converter and full C++ implementation (#26190, `1cbfd19`) -- notable new multimodal audio capability
- **model**: add Nanbeige4.2 support with new arch and converter (#25994, `b77d646`)

## CUDA / GPU / HIP

- **HIP**: disable `-ffast-math` compiler flag; was causing correctness issues with floating-point math on HIP targets (#25495, `91f8c9c`)

## SYCL / Build

- **sycl(build)**: parallelize `ocloc` invocations to speed up SYCL kernel compilation (#25903, `8e8681e`)

## CPU / Performance

- **ggml-cpu**: enable BF16 tiled GEMM optimization on PowerPC (#26068, `b910200`)

## GGML / Backend

- **ggml**: rework op offloading logic to use the weight tensor's backend instead of the compute tensor's; includes DeepSeek v4 graph fixes (#25832, `dee2a84`) -- could affect multi-GPU op placement behavior

## API / Common

- **common**: add `common_print_available_devices()` helper, refactored out of `llama-bench` (#26170, `0e4a036`)
- **args**: add `-lm mlock` mode that mlocks model weights without using mmap (#26135, `ad256de`)

## Server / UI

- **server + ui**: fix stream resume routes breaking when model names contain a slash (e.g. `ggml-org/...`); also fixes cancel-during-load and resume-after-reload edge cases (#26137, `d73c1d6`)

## Speculative Decoding

- **common**: fix `-md` explicit draft model path being silently overridden by sidecar auto-resolution; explicit draft now disables sidecar lookup (#26165, `ddfc228`)

## Fixes / Misc

- **fit**: count MTP (nextn) blocks toward `n_gpu_layers` so front model layers stay on GPU (#26177, `0324696`)
- **tests**: remove unnecessary sync in `test-save-load-state` (#26166, `7ef790f`)
- **docs**: document weight-folding exception in model addition guide (#26168, `419b881`)

---

## AI Policy Report

4 PRs were closed without merging in the last 24 hours. Of those, 3 (#26191, #26192, #26193) explicitly disclosed AI usage in their descriptions. Full closure reasons could not be confirmed (GitHub MCP access is restricted to the fork), but at least PR #26202 ("context: add cumulative mean pooling state") showed no AI usage disclosure in its body while containing a verbose, AI-sounding description -- a pattern consistent with policy closure. The 3 PRs from the same author (#26192, #26193) appear to have been closed as superseded or incomplete (both related to Kimi-K3 support still in progress under #26185).

**Estimated PRs closed for improper AI policy usage: 1** (PR #26202; others closed for scope/completeness reasons).
