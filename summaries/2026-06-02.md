# llama.cpp daily summary — 2026-06-02

Very active day: 21 commits merged to upstream/master spanning Vulkan/SYCL/Metal/OpenCL/WebGPU improvements, a notable tensor-parallel quantized KV cache feature, EXAONE 4.5 model support, speculative decoding fixes, and CI cleanup.

## Tensor Parallel / Core

- **TP: quantized KV cache support** — extends `ggml_backend_meta_split_state` to track segment repetition so KV shapes survive flatten/reshape without data layout loss; no compute graph changes required (#23792, `8e6fff8`) ⚠️ notable feature
- `llama_context`: limit `n_outputs_max` to `n_seqs` when possible, saving ~1.2 GB+ VRAM with large ubatches (#23861, `de6f727`)

## Vulkan

- Block-load Q3_K/Q6_K block data; subtract on 32-bit ints instead of i8vec4 — ~57% / ~78% tg128 speedup on Intel BMG Xe2 (#23056, `1962000`)
- Don't hold device mutex during pipeline compilation; use per-pipeline lock to allow concurrent shader compiles (#23641, `55ac090`)
- Reduce host memory lock contention: swap `unique_lock` → `lock_guard` in hot paths (#23376, `bef69f1`)
- Remove unused functions cleanup (#23175, `f8c0a19`)

## Metal

- Template GLU kernels (REGLU/GEGLU/SWIGLU etc.) to support f16/f32 natively; ALU stays in f32 to avoid precision issues, saves memory bandwidth (#23882, `95b8b8e`)

## SYCL

- Flash-Attention: add Q4_1, Q5_0, Q5_1 quantization support (#23812, `a511424`)
- GET_ROWS op: add ~17 new quant types (IQ1/IQ2/IQ3/IQ4 families, Q2_K–Q6_K, MXFP4, NVFP4) (#23710, `4162522`)
- Optimize Q3_K `mul_mat` via block reordering (#23725, `44e211c`)

## OpenCL

- Add basic Q5_0 and Q5_1 quantization support with non-uniform workgroup size (#23548, `27d9ed8`)

## WebGPU

- Revert cpy shader to `global_invocation_id` (drops `linear_indexing` extension); fixes Safari inference regression (#23955, `b8275a8`)

## Models

- **Add EXAONE 4.5** — includes GQA, vision support (Qwen2.5-VL-style encode path with `<vision>` markers), and MTP/NextN tensor loading (#21733, `48b88c3`) ⚠️ new model

## Speculative / Server

- Fix `n_outputs_max` sizing for implicit draft-simple; extract `common_speculative_n_max()` helper; remove draft-simple auto-enable; re-enable server tests on PRs (#23988, `5dcb711`)
- Support manually triggering reasoning budget end sequence (#23949, `5254a79`)

## Build / Infra

- vendor: update cpp-httplib to 0.46.1 (#23980, `335abed`)
- nix: add nix-nodejs build facilities for Web UI, preventing huggingface.co download (#23846, `5aa3a64`)
- CI: remove redundant/duplicate jobs to stay within the 20-job GitHub limit (#23927, `af6528e`)
- CI: fix missing Linux label on cpu-x64-high-perf runner (#23958, `e22b0de`)

## Misc

- vocab: add `normalizer.lowercase` support for WPM tokenizer (#23899, `5aba536`)
- security: disable private vulnerability disclosures in favor of public patching (#23963, `02a5701`)

---

## AI Policy Report

Review of unmerged PRs closed in the last 24 hours for AGENTS.md violations:

- **PR #23962** ("Merge turboquant"): Closed without merge. Body is entirely the unfilled PR template with every available label applied — strong signal of an automated/AI-generated submission.
- **PR #23987** ("server: fix n_outputs_max sizing for implicit draft-simple"): Closed without merge. AI disclosure reads "YES Opus 4.8 High + rootless container with full CI loop" — describes a fully agentic, automated PR submission, a prohibited category under AGENTS.md.
- **PR #22821** ("HIP: Adds 4x packed Q8_1 activation for Q4_K_M models in MMVQ"): Closed without merge after 12 review comments. Author disclosed AI use for implementation ideas and comments; may have been closed for other technical reasons.

**PRs closed due to improper AI policy usage: ~2 confirmed (#23962, #23987); 1 ambiguous (#22821).**
