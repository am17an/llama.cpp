# llama.cpp daily summary -- 2026-06-19

A busy day dominated by server and multimodal (mtmd) work: 24 commits landed, touching
SYCL GPU ops, Qualcomm Hexagon tracing, server schema validation, and an ongoing mtmd
preprocessor refactor. The SYCL Level Zero rename (#24719) is a breaking change for
downstream build scripts that reference those CMake/preprocessor symbols.

## Multimodal (mtmd)

- Refactor preprocessor, introduce `mtmd_image_preproc_out` struct; remove unused clip API -- #24736 `24bba7b`
- Refactor llava-uhd overview image handling to always use `ov_img_first` path -- #24769 `060ce1b`
- Add batching support for InternVL model; serves as reference for porting other models -- #24775 `db52540`
- Add batching in `mtmd-cli`; expand test script to cover video inputs -- #24778 `a6b3260`

## SYCL

- **Breaking rename:** `GGML_SYCL_SUPPORT_LEVEL_ZERO` -> `GGML_SYCL_SUPPORT_LEVEL_ZERO_API` and `GGML_SYCL_ENABLE_LEVEL_ZERO` -> `GGML_SYCL_USE_LEVEL_ZERO_API` -- #24719 `9724f66`
- Support `MUL_MAT` and `OUT_PROD` with Q1_0 quantization -- #24721 `dd69db2`
- Add `conv_2d`, `conv_2d_dw`, `conv2d_transpose` ops -- #24600 `6f1034b`

## Hexagon / Qualcomm

- Add fine-grained HVX/HMX/DMA op-tracing with Perfetto output; new `ggml-hexagon-trace.py` script -- #24592 `d2c6795`

## API / Server

- **Notable:** Add request schema validation in new `server-schema.cpp`; proper field-level error messages -- #24150 `e1efd09`
- Rework `-hf` preset repo: single `preset.ini` per HF repo replaces per-model attach pattern (breaking for anyone using the old preset URL scheme) -- #24739 `552258c`
- Fix router child instances not receiving parent CLI args (regression from #23976) -- #24760 `968c438`
- Fix `stopping_thread` potential hang in router mode -- #24728 `fe7c8b2`
- Add `X-Accel-Buffering: no` header on streaming endpoints (prevents Nginx from buffering SSE) -- #24774 `40f3aaf`
- Return HTTP 400 on invalid grammar instead of silently dropping constraint -- #24154 `1078621`
- Add rolling 5-second generation speed display in server context -- #24291 `0802307`

## Metal

- Runtime BF16 support check in concat kernel to avoid crashes on older Apple GPUs -- #24747 `cae0a3b`

## CUDA

- Add `GGML_OP_COL2IM_1D` CUDA kernel using fast div/modulo index decomposition -- #24417 `3a3edc9`

## UI

- Touch-accessible model selector: action buttons always visible on coarse-pointer devices; desktop hover unchanged -- #24604 `2083217`

## Build / CI / Misc

- cmake: fix UI build when source directory is read-only -- #24752 `32eddaf`
- App self-update gated behind `llama-install.sh` build flag -- #24754 `6ec59dd`
- CI: fix check-release message parsing and Windows x64 OpenVINO release link -- #24751 `32e806b`, #24731 `4a79037`
- docs: fix `export-lora --lora-scaled` syntax -- #24703 `7b6c5a2`

---

## AI Policy Enforcement Report

**PRs closed for improper AI usage in the last 24 hours: 1 (suspected)**

- PR #24763 ("server : restore forwarding of base CLI args to router child instances") was
  closed without merging within minutes of submission by a first-time contributor. The PR
  body exhibits hallmarks of fully AI-generated content (verbose structured analysis,
  code-block walk-through, inability to test locally). The underlying issue was valid and
  was addressed the same day via PR #24760 by a project member. This PR was not confirmed
  via close-comment inspection (upstream repo comments are outside this session's API
  scope), but the pattern is consistent with an AI policy rejection.

No other PRs closed in this window showed explicit AI policy rejection signals.
