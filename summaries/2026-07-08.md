# llama.cpp daily summary -- 2026-07-08

A busy day with 20 commits landing on upstream/master, spanning new quantization support, significant CUDA/SYCL improvements, server enhancements, and a batch of bug fixes. The SYCL backend received the most attention with six commits broadening its op coverage and correctness.

## Quantization

- **New Q2_0 quant type**: type definition, CPU backend (ARM + fallback), GGUF constants, and quantize tool support added -- a new 2-bit format distinct from Q2_K (`bec4772`, #24448)

## CUDA / GPU

- **NVFP4 MMVQ post-scale fusion**: fuses the scale/bias-add epilogue into the MMVQ kernel for NVFP4 models, yielding up to ~9% token-gen speedup on Qwen35MoE on Xeon; restricted to NVFP4 to avoid regressions on other quants (`3899b39`, #24481)

## SYCL

- Fix unsupported CONT/CPY unit-test cases (`108f186`, #25231)
- Add `col2im_1d` op support (`47e1de7`, #25264)
- Add `cross_entropy_loss` and `cross_entropy_loss_back` ops (`55edb2d`, #25236)
- Fix non-contiguous ACC unit-test cases (`95e5254`, #25124)
- Enhance argsort to pass all unit-test cases (`9e5ef0d`, #25125)
- Use SYCL built-ins to fix AOT double-type issue; rename env vars from "disable" to "enable" (`3d4cbdf`, `26145b3`, #25081, #25042)
- Set `K_QUANTS_PER_ITERATION=1` on DMMV path (`d209086`, #25063)

## OpenCL

- Flash attention decode performance optimizations (`6f8895f`, #25366)

## HIP

- Add `-fno-finite-math-only` alongside `-ffast-math` to fix NaN/Inf handling (`33ca0dc`, #25373)

## API / Server

- Fix draft-model fit-vs-load inconsistency in speculative decoding (`f5525f7`, #25056)
- Add timings and progress fields to `/responses` API streaming events (`5eca4e3`, #25348)
- Enforce prompt-cache RAM limit in server to prevent OOM (`6c487e2`, #25070)
- Fix speculative naming/spacing (`c198af4`, #25410)

## Fixes

- **llama**: Fix quantized KV-cache for DeepSeek-V4 -- Hadamard rotation was incorrectly applied to the quantized K path, producing garbled output; now bypassed for `LLM_ARCH_DEEPSEEK4` (`024c46a`, #25202)
- **speculative**: Fix out-of-bounds read in ngram-map on prompt shrink (`defa95c`, #23936)
- **Vulkan**: Check src0 type in `GGML_OP_SET_ROWS` to avoid f16 failures (`a8cfdbb`, #25351)

## Misc / Build

- `ggml_time_init` made idempotent (`1a7c25b`, #24422)
- Add missing `<fstream>` include in `common.h` for strict toolchains (`c1a411f`, #25220)

---

## AI Policy Enforcement Report

Based on a search of PRs closed without merging in the last 24 hours that mention AI in comments or descriptions, an estimated **2 PRs** were closed for improper/predominant AI-generated contributions:

- **#25386** ("Add Quatfit1 architecture") -- closed within ~1 hour of opening; heavily AI-drafted description with no demonstrated understanding; author is a first-time contributor with NONE association.
- **#25391** ("fix DeepSeek-V4 quantized K-cache garbage") -- closed ~1.5 hours after opening; highly verbose AI-style write-up; note that the underlying issue was legitimately fixed in #25202 by a regular contributor.

(Note: GitHub read access for this session is limited to the fork; exact closure reasons could not be verified from PR comments.)
