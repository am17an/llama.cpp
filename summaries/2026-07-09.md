# llama.cpp daily summary — 2026-07-09

Today was a heavy GPU-backend day: 21 commits landed spanning OpenCL, Vulkan, WebGPU, Hexagon, Metal, and CUDA, alongside meaningful improvements to the CLI architecture, server streaming, and the web UI. A security fix for the UGM tokenizer and a correctness fix in the SIMD GEMM kernel are the most notable safety-relevant changes.

## OpenCL

- **Q6_K GEMM/GEMV fix** for `ne01` not a multiple of 128 (Adreno): routes mis-aligned shapes off the noshuffle path and reserves alignment slack to prevent silent memory corruption in the SOA subbuffer carve — **data-corruption bug fix** (#25464) `92366df`
- **Ragged-tile MoE prefill FP16 GEMM optimization**: skips fully-padded expert tiles at quarter-granularity (8-col skip-groups) across all 8 `*_f32_ns` MoE GEMMs; tunable via `GGML_OPENCL_MOE_RAGGED_FP16` (#25433) `167d057`
- **Potential crash fix** in AOS reconstruct (#25383) `931ca30`

## Vulkan

- **Disable FA `mask_opt` on GCN GPUs** to improve performance (re-enable for head-size > 256) (#24362) `a646006`
- **Reduce submission threshold for small AMD GPUs** based on CU count (#25240) `0bbc87b`

## Hexagon (DSP)

- **Major MUL_MAT / FLASH_ATTN_EXT refactor**: new VTCM layouts with overlapping areas, improved DMA pipelines, inline-asm HMX macros replacing intrinsics, hybrid polling in workpool, async HMX queue suspend (#25425) `81ff7ab`

## WebGPU

- **Flash-attn vec tuning**: tune `d_split` subgroup split for `flash_attn_vec` shader (#25418) `32e41fa`

## Metal / CUDA / CPU

- **Metal**: add `set_rows` with `src0 f16` (#25434) `0512ef1`
- **CUDA**: add support for `f16→f16 GGML_OP_SET_ROWS` (#25367) `ed8c261`
- **CPU**: add `f16→f16 GGML_OP_SET_ROWS`; refactor into shared `_impl()` (#25344) `68a521b`
- **SIMD GEMM correctness fix**: incorrect A-matrix index in scalar tail-column path (#25390) `57b50e1`

## API / Server

- **CLI moves to HTTP-based implementation**: the `llama-cli` tool now drives the server via HTTP rather than calling internal APIs directly, enabling router-mode and remote-server usage (#24948) `c264f65`
- **SSE Replay Buffer follow-up**: pimpl, mutex-guarded session state, trimmed comments (#23226/#25047) `bbebeec`

## Core / llama

- **Fused ops refactor**: refactors fused op handling in `llama-context` for cleaner graph building (#24646) `90e0f5c`
- **llama-batch**: fix allowed decreasing `pos` in a sequence (#25449) `1ee0939`
- **llama-batch**: add `n_keep_tail` in `split_equal` for recurrent models (#25278) `230ea9d`

## Web UI

- **Context usage gauge and panel**: new `ChatFormContextGauge` component with fresh/cached token breakdown, hover-card details, and refactored token tracking infrastructure (#25340) `f1161b1`

## Security / Fixes

- **OOB reads in UGM tokenizer** (`precompiled_charsmap` handling): validate minimum size before reading `xcda_blob_size`, bounds-check string scanning — **heap-buffer-overflow from malicious T5/UGM GGUF files** fixed (#18750) `4a7ee31`
- **llama-eval**: fix crash when `answer` is `None` in HTML dump (#25435) `da46e59`

## Build / CI

- **HIP quality check** now runs on all changes, not just HIP-specific ones (#25403) `07e012a`
- **common**: auto-create `prompts-log-dir` at argument-parsing time so all tools using the flag benefit (#25322) `f296fdf`

---

**PRs closed due to improper AI policy usage:** 0
