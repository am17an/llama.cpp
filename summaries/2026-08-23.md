# llama.cpp daily summary -- 2026-08-23

Active day with 15 commits merged, led by a significant new JSON abstraction layer in common/, multimodal improvements, and performance work across Vulkan, SYCL, and CPU backends. No breaking changes identified.

## Common / API

- **New `json.h` abstraction** (#27511, `d9f918d2`) -- Large refactor adding a `common/json.h` wrapper over nlohmann JSON, migrating common, server, jinja, and tests to the new API. Reduces direct nlohmann exposure across the codebase.
- **Fix clang LTO in `json.h`** (#27575, `70adb1b4`) -- Follow-up fix for LTO build failures introduced by the json.h abstraction.
- **Fix draft-MTP with embeddings** (#27400, `2c6b141e`) -- Bug fix: speculative decoding (draft-MTP) was broken when used with embedding models.
- **Context fit: account for `n_streams`** (#27496, `2fb989b9`) -- Fit heuristic now factors in the number of streams when computing available context.
- **Remove `-no-cnv` CLI arg** (#27542, `9fee29e9`) -- Deprecated flag removed from the CLI.

## Multimodal (mtmd)

- **Use `ggml_rope_set_offset`** (#27521, `b21e4de7`) -- Refactors rope offset handling in clip/gemma4v/minimax-m3, simplifying the code and reducing line count.
- **Support dots3-note vision+audio** (#27524, `54ee5ee6`) -- Adds support for the dots3-note multimodal model (vision + audio inputs).

## GPU / Backends

- **Vulkan: add `PAD_REFLECT_1D` op** (#26586, `3f545bec`) -- Implements 1D reflection padding as a GLSL compute shader for the Vulkan backend; tested on Intel Iris Xe.
- **SYCL: re-add Q2_K MMVQ + ESIMD kernels** (#27490, `86722900`) -- Restores previously reverted SYCL performance kernels for Q2_K quantization, with gate params added.

## CPU / ggml

- **Optimize `concat` op** (#24575, `369e1cd6`) -- Replaces per-element `memcpy` with row-level `memcpy` in the CPU concat operation, improving throughput.

## Models

- **DSpark support for bailingmoe3** (#27508, `2115b73d`) -- Adds the DSpark model variant under the bailingmoe3 model family.

## CI / Build

- **Restore ROCm Ubuntu job** (#27399, `e85caa81`) -- Re-enables the ROCm CI job for Ubuntu.
- **ccache-clear enhancements** (#27504, `3a653fea`) -- Adds `older`, `min`, and `dry-run` options to the ccache-clear action.

## Misc

- README badge fixes and alt-text correction (`3aeb9246`, `2100e592`).

---

## AI Policy Report

PRs closed in the last 24 hours due to improper AI-generated content or automated submission violations: **0**
