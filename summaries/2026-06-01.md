# llama.cpp daily summary — 2026-06-01

A light day with 6 commits merged to upstream/master, covering a notable behavior change to iGPU device selection, a new tokenizer for Chinese embeddings, two server/UI fixes for ETag handling, a CI scope tightening, and a documentation update.

---

## Core / llama

- **iGPU: default to single device** — `llama.cpp` now restricts iGPU usage to one device by default, preventing unintended multi-iGPU selection. **Behavior change** for users with multiple integrated GPUs. (#23897, `22cadc1`)

## Vocab / Tokenizer

- **New tokenizer: jina-embeddings-v2-base-zh** — Adds whitespace-tokenizer support for the Chinese `jina-embeddings-v2-base-zh` embedding model, touching conversion scripts, gguf-py, and `llama-vocab`. (#18756, `d4c8e2c`)

## API / Server

- **Weak ETag support in `If-None-Match`** — Server now correctly handles weak ETags (prefixed `W/`) in conditional HTTP requests, improving cache-validation compliance. (#23916, `6f165c1`)

## UI / Build

- **MSVC ETag truncation fix** — Fixes a compiler-specific bug where ETags were being truncated under MSVC due to integer type handling in `embed.cpp`. (#23917, `3292da0`)

## Build / CI

- **CPU workflow trigger narrowed** — The CPU CI workflow no longer fires on GPU-kernel-only changes, reducing unnecessary CI load. (#23938, `399739d`)

## Docs

- **ZenDNN: Q8 quantization docs** — Updated `docs/backend/ZenDNN.md` to document Q8_0 quantization support in the ggml-zendnn backend, with links to official ZenDNN documentation. (#23791, `e6123e2`)

---

## AI Policy Report

During the past 24 hours, **2 pull requests** were closed without merging on `ggml-org/llama.cpp` that disclosed AI tool usage in their submissions:

- **#23712** — "server: expose n_prompt_tokens_target in /slots endpoint" (disclosed: reviewed with a local LLM)
- **#23225** — "ggml: allow split-mode tensor to use different quantization types" (disclosed: Codex used for navigation and sanity-checking)

Note: These closures were identified via search; closing comments on the upstream repository are not accessible from this session, so it is not confirmed that AI policy was the sole or primary reason for closure. No PRs were found with explicit AI-policy rejection labels during this period.
