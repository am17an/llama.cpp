# llama.cpp daily summary — 2026-07-18

Today's activity (11 commits) was dominated by GPU backend improvements across OpenCL, Vulkan, and SYCL, with a notable ggml version bump to 0.17.0 and a new speculative-decoding sidecar auto-download feature for the CLI.

## Notable / New Features

- **common**: Auto-download `dflash-` and `eagle3-` Hugging Face sidecars (#25811, `635cdd5`) — mirrors the existing `mtp-` sidecar logic; adds `--dflash` and `--eagle3` CLI flags and wires download tasks that configure speculative draft model params. Useful for zero-friction speculative decoding setup.

## OpenCL / GPU (Adreno / Qualcomm)

- Add `ABS` op to OpenCL backend (#25115, `b85833e`) — new kernel + CMake wiring.
- Transpose q4_K noshuffle scales for coalesced reads (#25805, `86d86ed`) — memory access pattern optimization reducing latency on Adreno GPUs.
- Vectorize MoE dp4a activation tiles as 128-bit loads/stores into local memory for Adreno (#25810, `6bdd77f`) — perf opt across five MoE kernel variants.
- Load `kernel_gemm_moe_q6_k_f32_ns` from binary kernel library (#25797, `86a9c79`) — offloads Q6_K MoE GEMM to pre-compiled kernel lib.

## Vulkan

- Support Q2_0 quantization format (#25430, `788e07d`) — adds mat-vec-mul and GEMM shaders for Q2_0; tuned rows-per-workgroup for competitive performance vs Q2_K.

## SYCL

- Fix row calculation when `K_QUANTS_PER_ITERATION` is 1 (#25690, `0bd0ec6`) — corrects off-by-one and missing second-half processing in several Q5_K / Q4_K dmmv kernels on Intel.

## BLAS

- Default hadamard `mul_mat` to CPU routine (#25710, `11fd0a6`) — prevents BLAS from being invoked for hadamard-product-style mul_mat, avoiding incorrect results.

## Build / Infra

- **ggml version bump to 0.17.0** (`3727404`) — notable milestone version; sync ggml submodule (`7d56da7`).

## Tests / Fixes

- Initialize all tensors in `test_dsv4_hc` to avoid NaN sentinel values (#25822, `5d5306b`).

---

**PRs closed due to improper AI policy usage:** 0

