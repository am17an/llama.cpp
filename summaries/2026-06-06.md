# llama.cpp daily summary -- 2026-06-06

A busy day with 18 commits spanning GPU backends, a notable new model (IBM Granite 4 Vision), and a broad internal refactor of `hparams.n_layer` touching 129 files. Performance improvements landed for CUDA MTP workloads on Blackwell and a large SYCL MMVQ port arrived from the CUDA backend.

## CUDA / GPU

- **CUDA**: Enroll `mul_mat_vec_q_moe` into PDL, yielding ~5-7% tok/s improvement for MTP (speculative decode) on Blackwell (B4500). (#24087, `2154a0f`)
- **SYCL**: Port multi-column MMVQ from CUDA backend; reads weights once per dispatch, covers Q4_0/Q8_0/Q3_K/Q4_K/Q5_K/Q6_K with reorder paths. Also fixes speculative/MTP failures that never triggered the reorder kernel. Large addition (~1100 lines). (#21845, `7fe2ae4`)
- **KleidiAI (ARM)**: Dynamic chunk-based scheduling for hybrid CPU execution. (#23819, `3ecfb15`)

## Vulkan

- Add FWHT support for Intel iGPUs using shared-memory reduction; disable subgroup shuffle on MoltenVK AMD and FWHT shader on Intel Windows due to driver bug. (#23964, `e82beaa`)

## OpenCL

- Improve `get_rows`, `cpy`, `concat`, and q6_K flat GEMV kernels; allow multiple workgroups for large rows and add packed concat for small inputs. (#24160, `308f61c`)

## Models / Architecture

- **Granite 4 Vision**: Add IBM Granite 4.1 Vision support in model + mtmd, including converter, tensor naming, and FEATURE_LAYERS hparam plumbing. Notable new multimodal model. (#23545, `64086f2`)
- **hparams refactor**: Rename/refactor `hparams.n_layer` across 129 source files - broad structural cleanup, no behavior change expected. (#24060, `7acb4e8`)
- **Tensor parallelism**: Round up TP granularity to 128 to avoid alignment issues. (#24180, `6effcec`)

## API / Chat

- Unify and fix LFM2/LFM2.5 tool parser; consolidates duplicated logic from `chat.cpp` into the PEG parser, adds tests. (#24178, `da87e9b`)

## Fixes

- CLI: fix model params not being propagated to inference. (#23893, `86591c7`)
- model: fix `llama_model::n_gpu_layers()` regression. (#24188, `96fbe00`)
- model: fix build failure introduced earlier in the day. (#24193, `c4a278d`)

## UI

- Run `npm install` when `package-lock.json` is newer than `node_modules`. (#24171, `2016bf2`)
- Add `ignore-scripts=true` to `.npmrc`. (#24149, `cc7bef3`)
- Fix broken link to UI settings docs. (#24169, `9c955c4`)

## Docs / CI

- Update quantization README. (#24133, `ad1b88c`)
- Slim down `build-msys` CI job dependencies, matching changes from whisper.cpp. (#24157, `46fa662`)

---

**AI policy violations**: 0 PRs identified as closed due to improper AI usage in the commits from the past 24 hours. (Tracking of policy-based closures requires monitoring PR activity directly on GitHub, which is outside the scope of the commit log.)
