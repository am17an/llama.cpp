# llama.cpp daily summary -- 2026-08-08

A busy day with ~27 merged PRs spanning the web UI, SYCL, Metal, server/router,
TTS/mtmd, and core ggml. The headline activity is a major web UI overhaul adding
a rich contenteditable chat input, slash commands, and filesystem @mentions;
and a significant SYCL push adding DeepSeek-V4 op support and multiple perf wins.

## Web UI / chat

- Rich contenteditable chat input with inline file-badge chips, undo/redo history,
  and per-code-fence splitting -- `fc6545d` (#26717)
- Slash command picker in the chat form with model-switching, raw-text toggle, and
  numeric settings clamping -- `6de1b63` (#26716)
- Filesystem `@`-mentions: floating search picker, badge rendering, keyboard nav,
  two new settings (full path / search depth) -- `2363478` (#26715)
- WebUI: honor `?model=...&load=true` to load the model on page open -- `f4f7758` (#26707)
- Read model input modalities from router list so image/audio attach works before
  `/props` loads (fixes vision model paste regression) -- `82bb485` (#26709)
- npm `min-release-age` guard against supply-chain attacks -- `34e9ee5` (#26711)

## SYCL / GPU

- **Perf:** Coalesce SSM_CONV window loads on Intel Arc; ~1.85x faster prefill on
  Qwen3.5 27B, +2.2% pp2048 -- `f8e3026` (#26612)
- **Perf:** GLU flat path (+14% f16 SWIGLU), fused-kernel consolidation -- `6b5c2ef` (#26354)
- **New ops:** DSv4 LIGHTNING_INDEXER, DSV4_HC_COMB/POST/PRE for DeepSeek-V4
  support on SYCL -- `31558db` (#26568)
- Expand `set_rows` to all missing data types (fp16->fp16/fp32, etc.) -- `c074cb3` (#26515)
- Fix FLASH_ATTN_EXT crash on Arc 770 -- `eef5f3e` (#26441)
- Fix UE4M3/NVFP4 scale parsed as signed E4M3 (was producing wrong scale values) -- `fc3f10b` (#25608)
- Update SYCL guide Q&A and device-setting scripts -- `c1f4109` (#26442)

## Metal

- **Bug fix:** NORM/RMS_NORM wrong output for row lengths that leave a partial
  simdgroup (e.g. ne0=33, 132, 260); partial sums were silently dropped -- `a194a75` (#26708)
- Fix `threadgroup half4x4` MSL compilation error in kernel_lightning_indexer -- `e40bf88` (#26646)

## Server / router

- Add LRU eviction scheduler; queues requests waiting for a model load and unblocks
  them together when ready -- `dff15d4` (#26572)
- Prevent eviction of models that are actively serving a request -- `e1470ee` (#26567)

## TTS / mtmd

- Fix Qwen3-TTS double-reading text (streaming overlay sent utterance a second
  time; now uses single tts_pad row matching the non-streaming prefill) -- `217df17` (#26706)
- Account for vocoder waveform pass in timing output so audio-per-second ratio is
  accurate -- `3653e6d` (#26733)
- Fix mtmd longest_edge preprocessor ignoring min/max pixel limits -- `4cb22cd` (#26638)

## Core / ggml

- ggml sync + version bump to 0.19.0 -- `4cf5cab` / `933f46f` (ggml/1581)
- aarch64: add HWCAP fallback definitions and require HWCAP_ASIMDHP for fp16
  variant detection -- `cb26014` (#25554)

## Build / CI / fixes

- CI: abort early when build requirements are missing; add Git LFS check -- `d8d9887` (#26368)
- Fix crash in `test-model-resolution` on Windows MSYS2 UCRT64 / GCC 16.1.0 -- `42e9881` (#26555)
- CUDA: suppress unused-variable/function warnings for non-Blackwell builds -- `5b87ed3` (#26688)
- Speed up server test suite 3x (shorter startup wait, load presets once per
  session, print 30 slowest tests) -- `1621a3d` (#26734)

---

## AI policy report

**PRs closed for improper AI usage in the last 24 hours: 1**

- #26698 ("ggml: comment out thread power throttling code") -- closed without merge.
  Submitted with an entirely empty PR description (template placeholders left
  unfilled, AI disclosure section blank), which is a common signal of a
  low-effort or fully AI-generated submission inconsistent with project policy.
