# llama.cpp daily summary -- 2026-07-25

A busy day dominated by WebUI performance work, OpenCL bug fixes, and Hexagon/Qualcomm backend improvements. 14 commits landed on upstream/master in the last 24 hours, with notable new AI coding skills added to the repo itself.

## WebUI / Server

- **Major streaming perf fix**: reduced per-token render cost from ~210ms down to ~2.67ms by rewriting the Svelte hot path, adding a performance harness and benchmarks -- #26053 `555881e`
- **Remove render effects**: eliminated viewport fade-in (caused invisible content in long agentic loops), replaced smooth autoscroll with instant, applied `content-visibility: auto` for offscreen message skipping -- #26083 `96013c5`
- **Settings precedence fix**: user settings now correctly win over admin (`--ui-config-file`) which wins over factory defaults; added a registry-driven invariant test suite -- #26002 `54ce507`
- **MCP display name conflicts**: fixed tool groups keyed by display name breaking the UI when two MCP servers share the same display name -- #26011 `95a923a`
- **OAI API**: added support for `"reasoning_effort": "none"` in the OpenAI-compatible server endpoint -- #26045 `27209a5`

## OpenCL / GPU

- **OpenCL binary cache**: cache compiled `cl_program` binaries on disk for Adreno GPUs (keyed by SHA-256 of source + options); observed up to 8.7x startup speedup on repeated runs -- #26050 `ed7adbf`
- **OpenCL flash attention bug**: fixed NULL-mask flash attention being incorrectly treated as causal, which caused multimodal (ViT) hallucinations when the OpenCL backend was used for mmproj offload -- #25771 `56a8386`
- **HIP**: removed rocWMMA-based FlashAttention (~800 lines deleted) -- #26046 `fa72aec`

## Hexagon (Qualcomm)

- **Hexagon pipeline overhaul**: extensive L2/DMA/matmul/flash-attention pipeline optimization including HVX softmax, double-buffered tiles, overlapped QK-dot and softmax, and improved tracing/profiling tools -- #26049 `0a50d99`

## Models / Chat

- **GLM 5.2 Indexer**: added support for the GLM 5.2 Indexer architecture, reusing the DeepSeek 3.2 graph with LLAMA_ROPE_TYPE_NORM rotary embeddings -- #25407 `88bfee1`
- **Cohere2 MoE template**: enforce JSON schema for text responses when a response schema is provided -- #26018 `8f5ab83`

## Fixes

- **llama bug fixes**: several OOB read/write fixes found via AI-assisted security analysis (GLM 5.2 used as the analysis model, with human review) -- #26051 `298219f`

## Docs / Skills / Tooling

- **New AI coding skills**: added `add-new-model` and `code-review` SKILL.md files to guide AI assistants on contribution standards -- #26042 `77095ee`
- **vendor**: updated `subprocess.h` to latest (adds CWD support, prep for stdio MCP) -- #26061 `0cea362`

---

## AI Policy Report

**PRs closed for improper AI policy usage (last 24h): ~1**

PR #26065 ("ggml-cpu: Enable Tiled Gemm Optimization for BF16,FP16 on PowerPC") was closed without merging approximately 1 hour after submission. The PR description was left almost entirely empty (template sections unfilled, AI disclosure blank), consistent with the pattern of automated or low-effort AI submissions that maintainers close under the AGENTS.md policy. No maintainer comments were accessible for confirmation, so this count is an estimate based on observable signals.
