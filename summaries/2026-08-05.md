# llama.cpp daily summary -- 2026-08-05

A busy day with 22 commits touching SYCL/GPU backends, multimodal (TTS support), sampler API changes, model loading, and several correctness/security fixes. Two entries are **breaking changes** worth attention.

## Multimodal / TTS

- **[BREAKING]** mtmd: support Qwen3-TTS; the `llama-tts` binary interface changed (#26254, `0713275`)
- mtmd: fix duplicate empty audio chunks for short inputs -- short audio was generating ~61 sec of frames instead of ~31 (#26536, `474c92e`)

## Sampler API

- **[BREAKING]** sampler: remove "full-context windows" from history-based samplers -- `penalty_last_n=-1` now resolves to 1024 (not ctx-len); default history window changed to 64; affects `libllama` callers (#26524, `a6aa6f5`)
- llama: move `n_vocab` from `llama_sampler_data` to `penalty_sampler`, matching pattern used by logit_bias/mirostat (#26520, `935cad6`)

## SYCL / GPU

- sycl: parallelize the non-contiguous concat kernel -- single-lane work-group widened; pp2048 +9.4% on Arc Pro B70 with Qwen3.6-27B (#25852, `6c8dcaa`)
- sycl: extend oneDNN SDPA to non-FP16 KV caches (Q4_0-Q8_0, FP32); F16 path unchanged (#25874, `66fa168`)

## Vulkan

- vulkan: implement `GATED_LINEAR_ATTN` backend op (#25601, `f26efa0`)
- ci: fix vulkan llvmpipe runs broken by `-march=native` + ccache variability (#26533, `22dc605`)

## Model loading

- model: allow tensor reshape at load time -- avoids extra graph nodes and fixes repacked-weight issues (#26531, `1269cb1`)
- models: fix `dflash wo_a` reshape missed in earlier load fix (#26577, `1c3c967`)

## Server / UI

- server: decode Windows OEM subprocess output to UTF-8 -- accented chars were silently replaced with U+FFFD on western Windows (#26597, `4308a4f`)
- ui: set CWD for agent tool (#26518, `2f56fc3`)

## gguf / Python

- gguf-py: validate `n_dims` and guard against `uint64` overflow in reader -- crafted files could trigger OOM or a silent silent undersized read (#25401, `5788b51`)
- gguf-split: add `--delete-splits` option to free disk during merge (#26538, `76c956c`)

## Conversion / Vocab

- convert: fix missing `return` after setting tekken vocab (#25947, `b5746d2`)
- convert: import `bytes_to_unicode` from `convert_slow_tokenizer` for transformers >= 5.15 compat (#26217, `b06fbc9`)
- vocab: validate plamo2 byte tokens (#26511, `cf06ad7`)

## Build / CI / Misc

- ci: fix pre-built macOS binaries broken on macOS 15 and below (#26375, `6b5224c`)
- build: prefer `npm ci` over `npm install` for reproducibility (#26601, `6ea215d`)
- speculative: refactor `common_speculative_init` to reduce duplication (#26510, `7bd8282`)
- ggml: bump version to 0.18.1 (`15831f5`), sync ggml submodule (`2e17f69`)

---

## AI Policy Report

PRs closed due to improper AI policy usage (fully AI-generated submissions violating AGENTS.md): **0 confirmed** in the last 24 hours.

Note: The GitHub search API does not expose individual PR close reasons or maintainer comments for the upstream repository from this session. The count above reflects cases identifiable via label or keyword search; actual closures enforced via maintainer comments may not be captured.
