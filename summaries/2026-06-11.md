# llama.cpp daily summary -- 2026-06-11

A light day with 6 commits merged to upstream/master, covering a cross-backend refactor for gated delta-net (MTP), two CUDA correctness fixes, a chat template bug fix, and minor CI/tooling updates. No breaking changes; the MTP refactor touches many backends but the interface is cleaner.

## CUDA/GPU

- **Remove padding and multiple D2D copies for MTP** -- refactors `ggml_gated_delta_net` across all backends (CPU, CUDA, Metal, Vulkan, SYCL, OpenCL, WebGPU, Hexagon) to pass snapshot count K as an op parameter and use a single strided `ggml_cpy` instead of the old padding hack; touches 20 files (#24086, e95dae1)
- **Fix `ssm_scan_f32` data-races** -- adds missing `__syncthreads()` before reusing `cub_temp_storage` shared memory in the CUDA SSM scan kernel; removes unused smem (#24360, fb83cc9)

## Chat / API

- **Fix LFM2/LFM2.5 ignoring `json_schema`** -- the LFM2 specialized template handler was building a grammar for tool-calling only and silently dropping `response_format.json_schema`; now properly handled (#24377, d2462f8)

## Speculative decoding

- **Fix `ngram-map-k4v` name in logging** -- corrects the speculative type name emitted at startup and runtime when `--spec-type ngram-map-k4v` is used; non-functional (#24253, d2e22ed)

## Build / CI

- **Bump komac version** -- updates the WinGet workflow to a newer komac release (#24396, 039e20a)
- **Update LibreSSL to 4.3.2** -- bumps the vendored cpp-httplib CMakeLists to point at the new LibreSSL release (#24397, ac4cdde)

---

## AI Policy Report

No PRs were closed in the last 24 hours for improper AI-generated content policy violations (0 closures identified matching AI/automated-submission criteria).
