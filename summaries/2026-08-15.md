# llama.cpp daily summary -- 2026-08-15

Active day with 15 commits landing on upstream/master. Highlights include a new MiniMax model family, notable SYCL performance wins for dense FFN and gated-delta-net ops, a cross-backend recurrent state rollback for SSM models, and a server concurrency improvement that unblocks /metrics and /slots during inference.

## Models

- **New model: MiniMaxText01 / MiniMaxM1** -- full support added including architecture, conversion, jinja chat template, and logits-mask handling for token suppression (#27018, `16d222f`)
- **Granite4 Vision fix** -- corrected image sequence assembly (grid layout, truncation for scaled height/width, removed dead add_newline code) in mtmd (#26653, `9d57ce4`)

## Server / API

- **Non-blocking /metrics and /slots** -- refactored server queue so these endpoints can respond while llama_decode() is running (#27041, `77918ca`)
- **reasoning_effort in chat templates** -- passes reasoning_effort through common_chat_templates_inputs so jinja templates can act on it; server reads it from the request body (#26941 / `7e4c0a9`)

## SYCL / GPU

- **Fused mul_mat(gate)+mul_mat(up)+GLU for q4_K dense FFN** -- 2-12% TG speedup on Intel Arc (higher gains at larger batch sizes) (#26779, `6509138`)
- **Fused gated-delta-net state writeback** -- ~1.2% TG improvement on Arc Pro B70 (#26643, `3d93885`)

## GGML Core

- **Recurrent state rollback for ggml_ssm_scan** -- enables RS rollback across CPU, CUDA, Metal, SYCL, Vulkan, and WebGPU backends; unsupported backends fall back to CPU (#26623, `1692f9e`)
- **ggml version bump to 0.20.0** (`06ae232`)
- **Virtual igpu devices** -- llama now accepts virtual integrated GPU device handles (#26953, `4c1a0af`)
- **WASI: force single thread** -- prevents ggml-cpu from spawning threads on WASI targets (#25686, `c6f6a92`)

## Fixes / Misc

- **jinja perf fix** -- gather_string_parts was O(n^2) due to repeated string concatenation; fixed to O(n) (#27034, `9e40df6`)
- **mtmd/common various fixes** -- minor corrections to clip and dflash code (#27071, `6fed9f6`)
- **dflash logging** -- clarified target_layer_ids output to a single readable line (#27013, `2bacf9e`)
- **Test path cleanup** -- replaced developer-specific /home/\<user\>/ paths with generic placeholders (#27043, `885c5bb`)
- **ggml sync** (`9b05354`)

## AI Policy Violations

Search of ggml-org/llama.cpp for PRs closed without merging in the last 24 hours returned 2 results: PR #27070 (release prep branch, closed by maintainers) and PR #25675 (disaggregated prefill, closed after review). Neither body indicated AI-generated content as the closure reason, and PR #25675 explicitly disclosed "AI usage: NO". **0 PRs confirmed closed for improper AI policy usage** in this period. (Note: comment-level closure reasons are not accessible via the search API used here.)
