# llama.cpp daily summary -- 2026-08-12

Active day with 22 commits merged to upstream/master, covering CUDA/GPU kernel improvements, chat template fixes, CI maintenance, speculative decoding cleanup, and several model/conversion updates. Notable highlights include a new CUDA WKV7 kernel, smarter CUDA graph disabling, and a bug fix for EXAONE 4.5 SWA not being enabled.

---

## CUDA / GPU

- **CUDA: only disable CUDA graphs when mul_mat_id actually needs a stream sync** -- previously graphs were disabled too aggressively; now only suppressed when a stream sync is truly required. PR #26802 (`ebb546b`)
- **cuda: add warp-per-row WKV7 kernel for single-token decode** -- new optimized kernel improves single-token decode throughput for WKV7 models. PR #26111 (`5988633`)
- **ggml-webgpu: fix CI errors from #25025 and #25262** -- adds i32 support to cpy, fixes subgroup matrices when max_kv_tile == 0. PR #26566 (`84f7129`)

## Chat / Templates

- **chat: fix muse-glimmer detection of tool calls after EOM** -- muse-glimmer was absorbing tool call markup into content when the model answered and called a tool in one generation; fix stops parsing at `<|eom|>` and extracts tool calls correctly. Adds template and four parser tests. PR #26879 (`0b1bad1`)
- **chat: tighten bare function parsing for Qwen models** -- prevents false positives in bare function call detection. PR #26793 (`ba360ef`)
- **common/peg: suppress incomplete escape sequences** -- avoids parser errors on malformed escape sequences in JSON strings. PR #26780 (`48d22e2`)

## Models

- **model: fix SWA not being enabled for EXAONE 4.5** -- `block_count=65` + `nextn=1` caused the SWA check to evaluate 65 (not 64) before `nextn` was read, skipping SWA entirely despite correct `n_swa` values in logs. PR #26848 (`14e78dd`)
- **llama: add default load-mode auto, avoids mmap on iGPUs** -- new auto mode detects iGPUs and skips mmap to avoid VRAM pressure. PR #26081 (`153d324`)
- **convert: keep quantization scales for nemotron --mtp export** PR #26903 (`5d16e81`)
- **Dflash support for nemotron-3.5** PR #26905 (`cc078b4`)
- **mtmd: support pocket-tts** -- adds multimodal support for pocket-tts model. PR #26871 (`6e62ba5`)

## Speculative Decoding

- **spec: update speculative-simple** -- simplifies `common/speculative.cpp`, cleans up the example, and updates docs. PR #26904 (`f785fc9`)

## Server / UI

- **ui: fix context gauge for single-model usage** -- context gauge was broken for non-split-model setups. PR #25738 (`8d274dd`)
- **tests: clean-up server test, use `tests.sh` in CI** PR #26886 (`b3df572`)
- **tests: fix running server tests on windows** PR #26889 (`9afff1b`)

## Build / CI

- **ci: add windows-rocm to check-release** PR #26897 (`70dfba5`)
- **ci: add missing release check** PR #26923 (`7b13a84`)
- **ci: hip-quality-check: update vgpr spill ignore list** PR #26859 (`7044859`)
- **tests: disable backend sampler HIP multi output** -- TOP_K-dependent tests abort on HIP (no CUB); added to skip list. PR #26878 (`4801e3c`)
- **requirements: use stable torch packages on s390x** PR #26864 (`2468576`)

## Misc

- **model-conversion: use save_output_data for causal embeddings** PR #26890 (`1138b85`)
- **imatrix.cpp: move finite check and only check touched experts** -- improves correctness and performance of imatrix computation. PR #26861 (`38406d5`)

---

## AI Policy Report

**2 PRs were closed unmerged in the last 24 hours for potential improper AI usage:**

- **PR #26924** ("Dsv4 sm tensor ours") -- Closed 2026-08-11. Submitted with a completely blank body (bare template, no description, no content). Title is cryptic and non-descriptive. Likely an automated or AI-generated submission with no human authorship evident.
- **PR #26915** ("common : handle missing gbnf regex anchors with .* fallback") -- Closed as draft 2026-08-11. Contributor disclosed AI was used "to automate the Git CLI branching workflow and format" the patch, which falls under prohibited automated commit/submission activity per AGENTS.md.

Total PRs closed due to improper AI policy usage (last 24h): **2**
