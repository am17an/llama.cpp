# llama.cpp daily summary -- 2026-07-01

Nine commits landed on upstream/master in the last 24 hours, heavily focused on GPU correctness
fixes across CUDA, HIP, Vulkan, and WebGPU. A notable scheduler revert stands out as a
stability-related rollback.

## CUDA/GPU

- **Fix integer truncation/overflow in KQ mask strides** for the `flash_attn_mask_to_KV_max`
  kernel; prevents silent data corruption on large sequence lengths. (#24945, `0eca4d49`)
- **Fix `get_rows_back` for embedding tables >65535 rows** by adding grid-Y clamping and
  proper stride handling. (#25103, `931eb37f`)
- **Fix Gemma E4B MTP FlashAttention** on CUDA; broken MTP inference path for that model
  family. (#25148, `e495d1e7`)

## HIP/ROCm

- **Use hipBLAS for dense prefill on gfx900, keep MMQ for MoE** -- improves throughput on
  older RDNA hardware while preserving MoE routing performance. (#24588, `d9df1100`)

## Vulkan

- **Roll `bk` loop in matmul for Asahi Linux** -- workgroup-level loop unroll fix for the
  Apple Silicon Vulkan driver; addresses correctness/perf issues on that platform.
  (#24663, `f708a5b2`)

## WebGPU

- **Add NVFP4 support to ggml-webgpu** -- enables inference with NVFP4-quantized models
  through the WebGPU backend. (#25143, `6c5de1cc`)

## Model support

- **Register `t_layer_inp` for Qwen3Next** -- missing tensor registration that caused
  inference failures with that model variant. (#25141, `4f31eedb`)

## API / Server

- **Handle bracketed IPv6 literals in URL authority** -- `common` and `server` now correctly
  parse addresses like `[::1]:8080` in connection strings. (#25140, `799fcc04`)

## Scheduler / Perf

- **Revert "sched: reintroduce less synchronizations during split compute"** -- the
  optimization from #20793 caused regressions and has been rolled back pending a cleaner fix.
  (#25138, `86b94708`) _(watch this space -- the original change was a perf win and will
  likely return in a revised form)_

---

## AI Policy Report

**1 PR closed in the last 24 hours due to improper AI usage:**

- **PR #25155** ("convert: add --temp-dir option for custom temp file location") -- flagged by
  the automated bot as fully or predominantly AI-generated and closed per the project's
  contribution policy.

An additional PR (#25159, "Microdavi support") was closed with no body content and no
maintainer comments; likely rejected on sight for being an empty/spam submission.
