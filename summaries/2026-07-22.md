# llama.cpp daily summary -- 2026-07-22

A solid day of backend improvements across GPU backends, server stability fixes, and multimodal corrections. 11 commits landed, with the most significant changes touching Vulkan queue threading, CUDA performance, and server error handling.

## CUDA/GPU

- **cuda**: add `sqrt_softplus` activation in topk-MoE kernel for DeepSeek-V4 (#25896) `846e991`
- **CUDA**: vectorize same-type `get_rows` with int4 copy for 27% throughput gain on large recurrent-state gathers; benchmarked at 18.6us -> 13.0us on Strix Halo (#25929) `305ba51`

## Vulkan

- **vulkan**: major refactor of `vk_queue` to use per-instance mutexes and unique handles; integrates `VK_KHR_internally_synchronized_queues` to bypass host-side locking when the driver supports it (#23570) `f048010` -- **notable**: large architectural change to queue ownership model

## Server / API

- **server**: return HTTP 400 (not 500) on validation error when `X-Conversation-Id` header is present; also fix UI sending placeholder string for `backend_sampling` on fresh profiles (#25760) `fd41bf6`
- **server**: properly handle null `llama_context` to avoid crashes (#25868) `40b740a`

## Multimodal / Vision

- **mtmd**: fix `align_corners` flag for Qwen3-VL position embedding interpolation -- mismatch caused grounding coordinate drift that grew with image size (#25781) `b4aa7dd`
- **convert**: fix handling of HunyuanVL XD-RoPE config during model conversion (#25514) `66e4bf7`

## Common / Speculative Decoding

- **common**: resolve draft repo to its requested sidecar when using `-hfd` with a repo that ships speculative sidecars; previously resolved to the main model instead (#25955) `60f6a17`

## Other Backends

- **hexagon**: check tensor type before reusing descriptors to avoid type mismatch (#25968) `71102a7`
- **kleidiai**: warn once (not repeatedly) when a weight type has no KleidiAI kernel (#25701) `fb0e6b6`
- **ggml-openvino**: add missing `GGML_BACKEND_DL_IMPL()` macro invocation, fixing dynamic loading for the OpenVINO backend (#25795) `5735e10`

---

## AI Policy PR Closures

PRs closed for improper AI policy usage (fully AI-generated submissions violating AGENTS.md): **N/A** -- this session is scoped to the fork (am17an/llama.cpp) and cannot query the upstream ggml-org/llama.cpp issue tracker directly. Check the upstream repo's closed PRs filtered by the "invalid" or "AI-generated" label for an accurate count.
