# llama.cpp daily summary — 2026-05-30

A busy day with 23 commits covering major new model support (DeepSeek V3.2 and DeepSeekOCR 2), several CUDA PDL bug fixes, WebGPU quantization improvements, and a handful of server/API quality-of-life changes. The DeepSeek V3.2 model addition is the largest single change (~1,260 lines) and may require attention from downstream users integrating custom DeepSeek backends.

## Model support
- **DeepSeekV32ForCausalLM** with generic DeepSeek Sparse Attention (DSA) implementation — large addition, new `src/models/deepseek32.cpp` (~500 lines) (#23346, `1f0aa2a`)
- **DeepSeekOCR 2** multimodal support via `mtmd` (#20975, `da3f990`)
- **LFM2.5-8B-A1B** tokenizer/vocab support (#23826, `2084434`)

## CUDA/GPU
- Check PTX version on host side before dispatching PDL kernels (#23530, `6ed481e`)
- Disable `launch_fattn` PDL enrollment due to compiler bug on DGX Spark (#23825, `241cbd4`)
- Fix missing `buffer` set in allreduce fallback for non-COMPUTE tensors — was causing corrupt output on Vulkan (#23480, `33c718d`)
- Ensure DS32 `kq_mask_lid` is F32 to fix type mismatch (#23864, `764f1e6`)

## WebGPU
- Add `q4_0`/`q8_0` `SET_ROWS` shader support with fast quantization path (#23760, `b22da25`)
- Check for required WebGPU features earlier in initialization (#23879, `151f3a9`)

## llama core / graph
- Use f16 mask for Flash Attention to save VRAM (~one mask buffer eliminated) (#23764, `031ddb2`)
- Add `llm_graph_input_mtp` for multi-token prediction graph inputs; refactors Qwen3.5/MoE models (#23643, `eef59a7`)
- ggml version bumped to 0.13.1 + sync (`ea02bc3`, `fe12e42`)

## API / server
- SSE mode: send HTTP headers as soon as a slot starts processing, reducing client time-to-first-byte (#23884, `0821c5f`)
- Bump server slot timeout to 3600 s (#23842, `cb47092`)
- Add `skip_download` option to download helper (#23059, `06d26df`)
- Add `speed-bench` script for speculative-decoding benchmarking (#23869, `689a9a4`)
- Remove obsolete server scripts (#23870, `b5f5228`)

## App / UI
- Add `llama update` self-updater subcommand (spawns installer detached so a running binary can be replaced) (#23865, `5a46b46`)
- Handle `audio/vnd.wave` MIME type as WAV in the UI (Firefox on Linux fix) (#23754, `22d66b5`)
- Move licence declarations into `llama-app` target (#23824, `98e480a`)

## CI / build
- Update macOS release CI runner to `macos-26` (#23878, `dc71236`)

## Misc
- Fix missing `#include` in `ngram-mod.cpp` (#23857, `b000431`)

---

## AI policy report

**3 PRs closed for improper AI policy usage** in the last 24 hours:

- **#23753** — "fix(cuda): propagate CUDA driver link dependency for shared builds" — PR body explicitly marked `(USER WAS BANNED FOR THIS POST)`; submitted by a contributor whose account was banned for AI-generated submissions.
- **#23766** — "fix(cuda): sanitize invalid Blackwell smpbo values" — same contributor (banned account); closed the same day without merge.
- **#23877** — "llama: drop redundant f16 cast for DSA mla/lid masks" — first-time contributor disclosed use of the Claude CLI; PR was closed by a maintainer within 13 minutes of opening.
