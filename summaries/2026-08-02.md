# llama.cpp daily summary -- 2026-08-02

7 commits landed on upstream/master in the last 24 hours. Activity spans Vulkan GPU optimizations, multimodal model support, chat/reasoning fixes, a vendor update, and a CI repair.

## Vulkan/GPU

- Extend `topk_moe` kernel fusion to support `sqrt(softplus)` activation, broadening the MoE fusion path for more MoE model variants (#26124, a7a6d0d)

## Multimodal (mtmd)

- Add MiniCPM-V 4.6 downsample mode; stores the downsample configuration inside GGUF, refactors the `minicpmv` preprocessor and adds a 4x VIT merger ignore path (#25993, de69995)

## Chat / Reasoning

- Persist `reasoning_content` in CLI chat history so `--reasoning-preserve` can re-inject prior thoughts on subsequent turns -- previously the reasoning was displayed but silently dropped from the stored messages (#26362, c629da5)
- Enable tool calls inside the thinking block for DeepSeek-R1 v4 (DS4); refactors the chat template parser to handle this case correctly (#26269, ddd4ec1)

## Agents / Docs

- Clarify AGENTS.md comment-style rules and expand Jinja template guidance in the code-review skill (#26405, 89482bd)

## Vendor / Build

- Update BoringSSL to `0.20260730.0` (#26353, 815a2a5)

## Testing / CI

- Fix CI errors: correct architecture test assertion and add missing `CMakeLists.txt` entries for the server UI build (#26415, 11924d4)

---

## AI Policy Report

In the last 24 hours, **5 PRs** were closed on ggml-org/llama.cpp without being merged (#20069, #20088, #25932, #26397, #26403). The upstream repository was not accessible via the configured GitHub MCP session for comment inspection, so the specific closure reason (AI policy violation vs. other) could not be confirmed for each. Based on available metadata, **0 PRs were confirmed closed explicitly for improper AI policy usage** in this period.
