# llama.cpp daily summary -- 2026-08-21

Active day with 24 commits to upstream/master, spanning Metal, OpenCL, CUDA, Vulkan, server, and core graph fixes. The headline addition is full quantized-KV support on Metal via a new F16 dequantization pass before flash attention, covering q4_0, q4_1, q5_0, q5_1, and q8_0 types. A revert of tensor-split meta-backend fixes (#26502) is notable -- the change was deemed incorrect and rolled back same-day.

## Metal / Apple GPU

- **Metal: dequantize quantized KV to F16 before flash attention** (#27390, `70aff252`) -- Major new capability: Metal FA now supports quantized KV caches (q4_0/q4_1/q5_0/q5_1/q8_0) by running a separate dequant kernel to F16 before the attention pass; V-is-a-view-of-K (MLA models) handled with a skip to avoid double work.
- Metal: clamp K extent in tensor API mat-mat kernel for K not a multiple of 32 (#27450, `a3027337`) -- Fixes UB/NaN on non-K-aligned shapes by using dynamic extents; adds test cases.
- Metal: dequant KV cache only for large batches (#27438, `63b64a50`) -- Perf gate: skips dequant overhead for small batch sizes.

## OpenCL

- opencl: fix q6_K flat mul_mat for Adreno A6x/A7x with older E031 compilers (#26476, `6503355d`) -- Extensive workarounds for four distinct codegen defects in E031.38/41; gated on compiler version. Also declines KV-convert FA variants on A7X where the driver crashes.
- opencl: support ggml_rope_set_offset on opencl, sycl, wgpu, hexagon (#27345, `749f688f`) -- Multi-backend RoPE offset propagation.
- opencl: fix local size for norm (#27339, `6b4fa88a`) -- Correctness bug fix.
- opencl: make MoE expert scatter deterministic (#26464, `9ee9fc04`) -- Fixes non-determinism in expert routing on OpenCL.

## CUDA / GPU

- CUDA: per-HW and per-quant-type switch points for mvq->MMQ crossover (#26079, `2b562109`) -- Tunable decode path selection for better throughput across GPU generations.
- ggml-cuda: static workspace for cuBLAS handles (#26574, `d9b6be07`) -- Avoids repeated workspace allocation; accounts for concurrent streams under CUDA graph opt.

## Vulkan

- vulkan: FA MMQ use fp32 for Q quantization (#27413, `78ec4c37`) -- Precision fix in flash attention.
- vulkan: add source groups for shaders (#26666, `2cfdb5fc`) -- Build/IDE organization.

## Core / ggml

- **Revert tensor-split meta backend fixes** (#27433, `f20395da`) -- Same-day revert of #26502; watch for a corrected resubmission.
- ggml: fix backend split scheduler race condition (#26040, `84979813`) -- Splits without inputs were running concurrently with others sharing memory; now synchronized.
- graph: create V as a view of K in k_iswa build_attn (#27392, `929d47a3`) -- Correctness fix for MLA models (DeepSeek4 MTP path).
- spec: avoid binding reference to null pointer (#27404, `f466cfa3`) -- Safety fix in speculative decoding.

## Server / API

- server: lazy-load startup_models after main setup (router, #27424, `9855ad69`) -- Avoids startup ordering issues.
- server: fix --docker-repo being treated as router mode (#27416, `8a832e4b`) -- CLI flag parsing bug.
- common: gracefully fallback on unsupported regex patterns in JSON schema (#26939, `dc64a162`) -- Robustness improvement.

## Models / Multimodal

- model: support DSpark for LFM2 models (#27383, `07822bdd`) -- New architecture support.
- mtmd: add --mmproj-device argument (#23255, `681c29d3`) -- Allows specifying the device for the multimodal projection model independently.

## Build / CI / Misc

- CI: use shell script for cmake pkg check (#27414, `0e1d9185`)
- CI: use LLVM OpenMP over MSVC_DEBUG on Windows (#26678, `bf0040e1`)
- convert: fix get block count error for Nemotron 3 Ultra (#27101, `a3b1effc`)
- ui: stores split refactor (#27240, `521a64cd`)

---

## AI Policy Report

Searching closed-but-unmerged PRs updated in the last 24 hours, **0 PRs were identified as explicitly closed for violating the improper AI-generation policy**. Several PRs were closed without merging (#27405, #27358, #27064, #26373, #11589) but their closures appear to be for scope, duplication, or quality reasons rather than AI policy enforcement, and none carried an AI-policy-violation label or explicit maintainer comment to that effect.
