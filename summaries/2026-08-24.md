# llama.cpp daily summary -- 2026-08-24

A moderately active day with 15 commits: a major UI feature (tabbed chat navigation), two model/backend bug fixes, a new CUDA kernel, and several test/CI improvements. No breaking changes detected.

## Models / Inference

- **GLM-4.5-Air MTP support**: add multi-token prediction support for the GLM-4.5-Air variant, updating the converter and model code (#26534, `c060ca97`)
- **DeepSeekV4 rollback fix**: fix KV cache rollback when multiple sequences share a context slot; adds a dedicated test for recurrent-state rollback (#26756, `b0539c43`)

## CUDA / GPU

- **POOL_1D kernel**: implement 1D average/max pooling in CUDA, previously only POOL_2D was supported (#27573, `29ea9412`)
- **Tensor parallel meta tensor fix**: correct split-state propagation for meta tensors in the ggml backend, preventing incorrect sharding in multi-GPU tensor-parallel setups (#27574, `d3371929`)

## Performance

- **Skip device_info loop when not needed**: avoid initializing GPU contexts (and the ~550 MB VRAM allocation CUDA incurs) when log verbosity is below TRACE (#26692, `ba8e0edd`)

## Server

- **LLAMA_SERVER_SLOTS_N_DIFF env var**: expose slot diffing threshold via `LLAMA_SERVER_SLOTS_N_DIFF` instead of a hard-coded value (#27600, `e8eed452`)

## Multimodal (mtmd)

- **Pillow-accurate resize**: switch to a Pillow-compatible bilinear resize algorithm and fix `resize_algo` for all multimodal models, simplifying the image processing path (#27594, `56db501e`)

## UI / Web

- **Tabbed chat navigation** (large): add browser-style conversation tabs to the web UI, with a scrollable carousel, keyboard shortcuts (Shift+Cmd/Ctrl+Left/Right to cycle, Ctrl+W to close), per-tab stop button, and a settings toggle; replaces the old `HorizontalScrollCarousel` with a unified `ScrollCarousel` component (#27263, `8144f319`)
- **Fix tab keyboard shortcut conflict**: update tab cycling shortcuts to avoid clashing with the rich input's word-jump binding (#27609, `d05f8956`)

## Tests / CI

- **Parser tests moved**: relocate `tools/parser` standalone binary into `tests/`, merging its test cases into `test-chat-auto-parser.cpp` (#27548, `4a08fa29`)
- **Multi-GPU server test fix**: guard slot-save and vision tests against multi-GPU environment differences (#27614, `8d9af256`)
- **Metal tensor-split CI**: run `test-llama-archs` with 1-4 `GGML_METAL_DEVICES`, mirroring existing CUDA multi-GPU coverage (#27598, `95b8e33e`)

## Docs / Contrib

- **Contributing: wait for CI**: add a note recommending contributors wait for CI to pass before requesting a merge (#27603, `a278dcef`)
- **README links**: refresh maintainer PR filter list and fix stale links (#27617, `ccc8fd2b`)

## Vendor

- **subprocess.h update**: sync `vendor/sheredom/subprocess.h` to a newer upstream revision (#27409, `6657ded4`)

---

## AI Policy Report

PRs closed in the last 24 hours for improper AI-generated content policy violations: **0**

(1 PR was closed unmerged -- #27611 "tests: update vision model for server test" -- but this was superseded by the broader multi-GPU server test fix in #27614, not a policy closure.)
