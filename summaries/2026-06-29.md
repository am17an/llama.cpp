# llama.cpp daily summary -- 2026-06-29

9 commits landed on upstream master today, spanning speculative decoding, chat/Jinja tooling, web UI fixes, and general cleanup. The headline addition is full DFlash speculative decoding support, a large feature that has been in review for a while.

## Speculative decoding

- **DFlash support** (#22105) `d1b3425` -- adds draft-model-based DFlash speculative decoding (+712 lines across 14 files including docs and conversion scripts). Notable new feature.
- **DFlash draft model conversion refactor** (#25110) `fa72bc6` -- cleans up the conversion pipeline for DFlash models (`conversion/llama.py`, `conversion/qwen.py`, gguf-py constants and tensor mapping).

## Chat / Jinja

- **`--reasoning-preserve` flag** (#25105) `b3fed31` -- new CLI flag in `common/arg.cpp` and `common/chat.cpp` to control whether reasoning tokens are preserved across turns.
- **MiniCPM5 chat parser** (#24889) `c818263` -- implements the MiniCPM5 jinja template parser (+482 lines); adds model template and extends test coverage.
- **`--dump-prog` for jinja debugging** (#25086) `f68a788` -- adds a debug flag to dump the compiled jinja program; touches `common/jinja/runtime.cpp` and test harness.

## API / server / UI

- **UI: fix stop and reasoning skip in single-model mode** (#25084) `7cb8576` -- corrects stop-generation and reasoning-skip behavior in the web UI's single-model path (`server-context.cpp`, `server-stream.cpp`, `chat.svelte.ts`).
- **Revert AI-assisted accessibility fix** (#25098) `dbdaece` -- reverts PR #24727 ("...assisted by claude"), which introduced regressions in UI component accessibility. Three Svelte components rolled back.

## Common / misc

- **`--offline` flag for model downloads** (#25091) `c1a1c8e` -- small patch to `common/arg.cpp` allowing `llama-download` to skip network requests and use cached files only.
- **Log reduction v2** (#25078) `27c8bb4` -- trims verbose logging across 12 files in `common/` (203 additions, 194 deletions); ongoing log-level cleanup from Georgi Gerganov.

---

## AI policy report

**PRs closed for improper AI usage in the last 24 hours: 1 (indirect)**

No PRs were explicitly rejected under the AGENTS.md AI-generated-code policy during this period. However, one AI-related revert was merged:

- PR #24727 ("ui: fix accessibility for hover-gated interactive elements **assisted by claude** (in debugging and tests)") was merged on 2026-06-26 but reverted on 2026-06-28 via #25098. The revert indicates the AI-assisted contribution introduced regressions, consistent with maintainer concerns about AI-assisted UI work lacking sufficient human review.

3 PRs were closed without merging today (#25104, #25101, #25090), but these were closed for unrelated reasons (duplicate contributor PRs per bot policy, not AI-generated code).
