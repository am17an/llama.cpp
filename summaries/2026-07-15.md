# llama.cpp daily summary -- 2026-07-15

A busy day with 21 commits merged to upstream/master, covering OpenCL correctness fixes, server refactoring and new CORS controls, Web UI improvements, ARM/GPU backend additions, and several ggml core cleanups.

## OpenCL

- Fix OOB write in noshuffle GEMV kernels for odd ne01 across 11 quantized kernel files -- #25640 `00fa7cb`
- Avoid vectorized GEMV path when row stride is unaligned (fixes incorrect results on Intel GPUs) -- #25671 `a4ce259`
- Fix dp4a fallback for devices lacking `cl_khr_integer_dot_product` (prevented backend init crash) -- #25639 `00e79f6`

## API / Server

- Refactor prompt cache state ownership: move prompt state data to `server_prompt_cache`, clear checkpoints on prompt clear -- #25649 `bf2c86d`
- Add `--cors-*` CLI options (`--cors-origins`, `--cors-methods`, `--cors-headers`, `--cors-credentials`); when `--agent` is active, defaults CORS to localhost only -- #25655 `6e52db5`
- Fix `seq_rm` in DeepseekV4 KV cache after checkpoint restore -- #25588 `7f575c3`
- Flush log before exit after `usage()` to prevent dropped log lines -- #25504 `ec0dbef`

## Web UI

- Fix MCP panel regressions: restore proxy switch, bound handshake with configured timeout, fix toggle visibility, fix per-chat server defaults -- #25631 `17a05e4`
- Add reasoning effort control to mobile add sheet (was desktop-only) -- #25539 `c9330ed`
- Fix spacing in tool-call permission request card -- #25634 `236ab57`
- Parse effective-parameter sizes (E2B, E4B) as model params in UI -- #25529 `dfba90d`

## Metal

- Add Q2_0 quantization support to Metal backend -- #25419 `14d3ba4`

## ARM / CPU

- Add SME2 f32 kernel for kleidiai with dynamic scheduling -- #24414 `47c7869`

## Vulkan / CPU

- Support f16 as SET_ROWS source in Vulkan and CPU backends; add Metal op check -- #25432 `7cbd610`

## SYCL

- Set `fattn_vec_nthreads` to 256 for Battlemage (Xe2) GPUs, keeping 128 as default for Alchemist -- #25205 `c1063ac`

## Hexagon

- Fix HMX-queue signal enum-narrowing problem (function pointer truncated to byte, misclassified as NOOP) -- #25677 `c718542`

## ggml core

- Add helper functions for checking contiguity of inner tensor dimensions -- #25650 `a7312ae`
- Uniformize `im2col` dst_type casting logic across all conv ops -- #23660 `47a3966`

## Tooling / Build

- Migrate `tokenize` tool to `common_params_parse`, removing hand-rolled argv parsing -- #25516 `8ff8c42`
- Fix `test-export-graph-ops` segfault when called without arguments -- #25619 `657e011`

## Conversion

- Support split MTP export for HY V3 in `convert_hf_to_gguf`; add `supports_mtp_export` capability to `ModelBase` -- #25641 `cb489bc`

---

## AI Policy Report

Among the 8 PRs closed without merging on 2026-07-14, an estimated **3 were closed due to apparent violations of the project's AI policy** (AGENTS.md / CONTRIBUTING.md):

- **#25642** ("deepseek4: NextN/MTP speculative decoding") -- PR description footer explicitly reads "Generated with [Claude Code]", a prohibited automated submission. Closed within ~2 hours.
- **#25651** ("metal: enable F16 support for leaky ReLU") -- AI disclosure stated AI "helped with code investigation and reviewing the change"; closed after ~22 minutes by a maintainer citing policy.
- **#25647** ("server: fix checkpoints not being freed") -- AI disclosure listed as "YES | Understand the code base"; closed citing that the proposed fix was superseded by an upstream commit and the AI-assisted framing.

Additionally, **2 long-pending PRs** (#18586, #18588, both from January 2026) were closed this day; their descriptions exhibit AI-generated formatting patterns (git commands embedded in PR bodies, CVE-style titles), though the closing comments cited stale/superseded status rather than explicit policy language.
