# llama.cpp daily summary -- 2026-09-04

A busy day with 18 commits merged to upstream/master, spanning GPU backends (Metal, CUDA, OpenCL, SYCL, Vulkan), a new model architecture, memory usage improvements, and a notable build fix. The Metal sparse flash-attention addition and a large OpenCL optimization batch stand out as the most impactful changes.

## Metal

- Add sparse flash attention (FA) kernel -- significant new feature for Apple GPU inference (#28098, `7bb0fc18`)
- Fix GLU dispatch when ne00 = 1 -- correctness fix (#28306, `0df017d6`)

## CUDA / GPU

- Allow concurrent streams per split for multi-GPU -- improves throughput on multi-GPU setups (#28198, `0ba6499c`)
- Tune MMVQ-to-MMQ crossover threshold for SM87 (Jetson Orin) (#28285, `8c1a2516`)

## OpenCL

- Major GEMV/GEMM optimization batch: quantized lm_head/decode GEMV and medium-batch GEMM for speculative decoding/MTP on Adreno GPUs; adds split-K decode, q4_K/q6_K/q8_0 kernels, tiled layouts, and GLU fusion (#26477, `95ef7fc1`) -- ~4800 lines added

## SYCL

- Add peer-to-peer copy API support (#27550, `0df974d7`)
- Reduce redundant work in Q4_K multi-column MMVQ (#27062, `4aa6ffba`)

## Vulkan

- Fix FA dequant path engagement -- was silently taking the wrong code path (#28190, `c7bda030`)

## Models / Conversion

- Add NVIDIA Nemotron-3-Puzzle-75B-A9B (NemotronHPuzzle) support with per-layer n_ff_exp/n_expert_used accessors; refactors MoE hparams across ~55 model files (#25444, `c61b98b8`)
- Skip bias_vl tensor in DeepSeek-V4 DSpark conversion to fix conversion error (#28294, `d646c9d1`)

## Build

- Fix full source rebuild triggered on every new commit by using a version header template instead of embedding the commit hash directly into source (#28278, `d230ddd7`) -- notable dev-experience improvement

## Server / Testing

- Switch server tests to pytest-xdist for parallel execution (#28298, `42f0225f`)

## Docs

- Add Hexagon backend to ops.md and README; includes new Hexagon.csv op coverage table (#28263, `e107984b`)

## Fixes / Misc

- Fix GBNF grammar generation for empty object schemas (#28279, `c5a5535e`)
- Prevent RAM peak at model loading stage by deferring allocations (#27483, `5ec4eab6`)
- Snapdragon CI updates to use new run.py script (#28293, `d30500b8`)
- mtmd: propagate const to preprocessor class (#28310, `de8656bd`); add const in various places (#28307, `f45576aa`)

---

## AI Policy Report

PRs closed in the last 24 hours due to improper AI-generated content policy (per AGENTS.md): **0**

No PRs were identified as closed for AI policy violations during this period.
