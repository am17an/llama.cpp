# llama.cpp daily summary — 2026-06-03

A productive day with 19 commits merged to upstream/master, spanning new model support, significant Hexagon/OpenCL backend work, server features, and API deprecations. The most notable changes are a new Mellum architecture, real-time reasoning interruption in the server, and the deprecation of `llama_set_warmup`.

## New Models / Architectures

- **Mellum architecture added** — full support for JetBrains' Mellum model family including converter, vocab, and graph builder (#23966, `4fb16ec`)
- **StepFun Step3.5 MTP support** — multi-token prediction for StepFun 3.5 models (#23274, `2187e00`)
- **Step3.7-Flash conversion** — converter support for Step3.7-Flash variant (#23845, `f7a0777`)
- **Granite multilingual embeddings R2** — support for `ibm-granite/granite-embedding-{97,311}m-multilingual-r2`; introduces `LLM_KV_HIDDEN_ACT` GGUF key for hidden activation type and SwiGLU FFN support in ModernBERT (#22716, `bfb4308`)

## API / Server

- **`llama_set_warmup` deprecated** — marked deprecated in `llama.h`; callers should stop using this function (#24009, `4f3a4be`) ⚠️ *API-level deprecation*
- **Server: real-time reasoning interruption** — new `POST /v1/chat/completions/control` endpoint with `reasoning_end` action to interrupt thinking mid-generation; opt-in via `reasoning_control` flag (#23971, `354ebac`)
- **Server: SSE ping interval** — keeps SSE connections alive to prevent proxy/firewall timeouts (#24013, `60130d1`)
- **KV-cache: SWA checkpoints store only non-masked cells** — reduces checkpoint size and improves unified KV cache restore for sliding-window attention (#23981, `2365315`)
- **Common: fix state save in `common_prompt_batch_decode`** — corrects state save logic (#23468, `0b71540`)

## Hexagon (Qualcomm HTP)

- **Broad GEMM/attention optimizations** — MUL_MAT, MUL_MAT_ID, FLASH_ATTN, and GDN cleanup for latest models, large changeset (1,800+ lines) (#23989, `8f7f3bf`)
- **GELU_QUICK op added** to Hexagon backend (#24007, `d178a11`)
- **Profiler output fix and script updates** — removes redundant NONEs, adds `tot.usec` column to profiling script (#24042, `5c394fd`)

## OpenCL

- **Use flat GEMV variants for large M** — q4_K and q6_K GEMV now uses flat (non-shuffle) kernels at high M values (e.g. vocab-size), improving performance (#24006, `63e66fd`)
- **Compiler warning fixes for non-Adreno path** — const-cast and unused-variable warnings cleaned up (#23922, `210a657`)

## UI (Web)

- **Thinking mode toggle** — new UI control for reasoning effort level in the chat form (#23434, `f8e67fc`)
- **Network error handling simplified** (#23431, `69cea5b`)

## CUDA

- **Unused-variable warnings cleaned up** across fattn, gated_delta_net, mmf, mmvf, mmvq, topk-moe CUDA files (#23975, `1fd5f48`)

## CI / Docs / Misc

- **CI: reduce self-hosted server workflow jobs** — lighter CI matrix for server tests (#24012, `a468b89`)
- **Docs: HOWTO-add-model.md updated** to reflect new model registration and graph-building patterns (#23883, `d5ab083`)

---

## AI Policy Report

**PRs closed due to improper AI policy usage (last 24h): 2**

- **#24041** — "ggml-hip: fix KV cache crash and disable Flash Attention on gfx1100 Windows ROCm" — closed with user ban notice; identified as AI-generated submission.
- **#24039** — "[AI policy violation] server: fix router child process lifecycle deadlock (dining philosophers)" — closed and labeled by maintainers as an AI policy violation.
