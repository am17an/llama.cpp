# llama.cpp daily summary -- 2026-08-27

Active day with 12 commits across GPU backends (Vulkan, CUDA, Metal), the RPC layer, the web UI, core KV-cache infrastructure, and CI/build. The two most notable changes are a large RPC async backend overhaul and a new Metal MMA kernel for Mamba-2 prefill.

## CUDA/GPU

- **CUDA**: unblock MMQ quantized matmul for MoE models on sm_60 (Pascal) GPUs by splitting the config into DP4A and non-DP4A variants -- #26264 `fc35562b`
- **Vulkan**: clamp warp-tile sizes to <= 64 to work around backends with larger warp widths (e.g. some RDNA/CDNA configs) -- #27726 `5e6a37cb`
- **Vulkan**: implement `cross_entropy_loss` and `cross_entropy_loss_back` ops in Vulkan shaders -- #27216 `bf942164`

## Metal

- **Metal**: add chunked SSD MMA kernels for Mamba-2 multi-token prefill optimization, with a sequential fallback tail -- #26647 `11cd9884` _(notable perf improvement for Mamba-2 models on Apple Silicon)_

## RPC / backend

- **RPC**: major rewrite of the RPC backend to implement the event and async backend APIs; also caches `RPC_CMD_GET_ALLOC_SIZE` responses -- #18626 `d0132a68` _(substantial change, +442/-165 lines in ggml-rpc.cpp)_
- **ggml-meta**: propagate buffer usage flags and call `init` on newly created tensors -- #27586 `5d5cb4c3`

## Core / llama

- **KV cache**: add per-cell token ID tracking (`get_prev_tokens` helper) to the KV cache, exposing it via `llama.h` -- #27762 `925e1179`

## Conversion

- **conversion**: fix Nemotron 3.5 Lightning layer-type matching (`full_attention` vs `attention`) that broke with transformers >= 5.5.1 -- #27729 `dac869b0`

## UI

- **UI**: move Settings and MCP Servers views from dedicated routes to dialog-based overlays; removes the `/mcp-servers` and `/settings` routes -- #27744 `539f2452`
- **UI**: update Dialog component styling with responsive sizing and sticky header support -- #27743 `0379a19f`

## CI / build

- **CI**: build the web UI once and reuse the artifact in release jobs; disable npm UI build by default (`LLAMA_BUILD_UI=OFF`) -- #27706 `4d19b287`
- **CI**: make the ccache S3 bucket public and add HF token gating -- #27728 `da9b5d68`

---

**PRs closed for improper AI policy usage (last 24h):** 0 confirmed. No PRs were identified as closed specifically due to the project's AI contribution policy in this period.
