# llama.cpp daily summary -- 2026-08-04

19 commits landed on upstream/master in the last 24 hours, with a heavy focus on MTP (Multi-Token Prediction) support for new models and Metal/GPU performance work. A notable heads-up: the server default port is changing from 8080 to 9931 (notice added in #26508) -- downstream integrations should prepare for this breaking change.

## Model support

- MTP support for DeepSeek V3.2 -- adds speculative decoding via multi-token prediction layers (PR #26457, `2b63e06`)
- MTP support for Qwen3-Next -- new architecture wiring with `opt_num_mtp_layers` flag (PR #25589, `4ed2b13`)
- MTP support for GLM-4.7-Flash -- reuses deepseek2.cpp backend with GLM-specific conversion (PR #24868, `57c0921`)
- M3 model: move MSA into a new memory implementation for cleaner architecture separation (PR #26338, `67d5978`)
- GLM indexer cache: allocate only in "full" indexer layers, reducing memory overhead (PR #26474, `563dec8`)
- Fix unused input tensors in minimax m3 graph (PR #26519, `c50b34a`)

## Metal

- **[Notable perf]** Implement DSv4 Lightning Indexer on Metal -- pp512 at depth 30k improves from ~33 t/s to ~49 t/s (47% gain); supports Q4_0/Q4_1/Q5_0/Q5_1/Q8_0/F16/BF16/F32 KV caches (PR #25893, `1464c62`)

## CUDA/GPU

- Add backend sampler for penalties sampler (PR #25262, `96278e3`)
- Fix data-races when reusing SMEM in block_reduce (PR #26385, `9bd4c09`)
- Use dynamic allocation for split graph inputs in ggml -- avoids fixed-size stack arrays (PR #22789, `dbadb68`)
- OpenCL: route large q6_K lm_head to the flat GEMV path (PR #26427, `39eab74`)

## API / server

- **[Upcoming breaking change]** Add notice that default port will change from 8080 to 9931 (PR #26508, `0b14b87`)
- Add `get_info` tool to server (PR #26522, `99111b1`)
- server tools: add `x-tool-cwd` header for tool working-directory context (PR #26420, `f2b52a8`)

## Chat / templates

- Add DeepSeek V4 Flash 0731 chat template; update V4 templates to match official encoder with structured output and reasoning effort support (PR #26398, `0ef6e55`)

## Core / vocab

- Validate default special token IDs at load time (PR #26506, `e8e06f7`)

## Tests

- Add model resolution test on synthetic repo listings (covers shards, mmproj, sidecar, presets) without network access (PR #26172, `ee0445c`)

## Vendor / build

- Update cpp-httplib to 0.52.0 (PR #26485, `94bc47f`)
- Update BoringSSL to 0.20260803.0 (PR #26523, `fe2adf0`)

---

## AI Policy Report

PRs closed due to improper AI policy usage (fully AI-generated or autonomous agent submissions, per AGENTS.md): **0 confirmed** in the last 24 hours.

Note: this session's GitHub access is scoped to the fork (am17an/llama.cpp) and cannot read comments on upstream ggml-org/llama.cpp PRs. The count above reflects what was detectable via PR search queries; the true figure may differ if violations were handled via comment-only closures not surfaced by search.
