# llama.cpp daily summary -- 2026-09-05

A busy day with 23 commits merged. The headline item is the **version 0.4.0 bump** along with a major ggml sync. GPU backend work dominated the day across OpenCL (Adreno, Qualcomm), Metal (M3/M3 Max), and SYCL, with a new Tencent Hy 4 model architecture landing as well.

## Release / Core

- **Version bump to 0.4.0** -- `5266f24` (#28386)
- **ggml sync** -- pulls in latest ggml changes -- `64a155d2` (#28379)

## Metal

- Add remaining fa-vec tunings for M3 -- `427291b5` (#28396)
- Add remaining fa-vec tunings for M3 Max -- `8f83678f` (#28373)

## OpenCL / Adreno

- **Extended elementwise and data-movement op coverage**: adds 9 new unary ops (sgn, step, elu, hardswish, hardsigmoid, floor, ceil, round, trunc), a fast contiguous f32 copy path dispatched over the whole device, and multi-type CONCAT support -- `1548a240` (#27633)
- **Adreno xmem SDPA path**: new subgroup-accelerated attention kernel for Adreno GPUs -- `4acf4a4c` (#26331)

## SYCL

- Fuse RMS_NORM+MUL+ADD and ADD+ADD residual chains under `GGML_SYCL_ENABLE_FUSION` for reduced kernel-launch overhead -- `6703d789` (#27610)
- Refactor `GGML_SYCL_ENABLE_MKL_FA` to a global variable -- `f9f09f02` (#26863)

## Models

- **Add Tencent Hy 4 (hy_v4) preview architecture support** -- new model family -- `49c0dc82` (#28127)
- Fix gemma4 vision handling in model/mtmd -- `163a4079` (#28335)
- Add `n_expert_used_max` helper to fix loading of per-layer expert models (NemotronHPuzzle) -- `9a4843cf` (#28323)

## API / Server / UI

- Fix MCP image attachments not displayed in tool block (regression from #25450) -- `85d5703a` (#28089)
- Export conversations from database instead of cached store -- `1863ac03` (#27432)
- Make build info output stream configurable; `llama-app --version` now prints to stdout -- `c390d0ab` (#28322)

## Build / CI

- Replace compile definitions with `version.h.in` to avoid unnecessary recompilation on version bumps -- `86b351fd` (#28364)
- Remove `GGML_CUDA_PEER_MAX_BATCH_SIZE` (obsolete option) -- `24f5bf8a` (#28177)
- Move more CI jobs to ccache buckets -- `8b4b3558` (#28375)
- Disable failing OpenVINO tests in CI -- `0ef4d560` (#28347)
- Update BoringSSL to 0.20260903.0 -- `38521ec3` (#28354)
- Don't use npx inside package.json script -- `d509cb1e` (#28270)

## Fixes

- ggml: don't crash when backend search path can't be read (inaccessible fs mounts) -- `4cbe8b07` (#28271)
- ggml-cpu (s390x): fix q5_1 uninitialized v_acc -- `832fd6f1` (#28332)

## Docs / Misc

- Update maintainer PRs link and regenerate AUTHORS -- `a529af96` (#28365)

---

## AI Policy Enforcement

**3 PRs closed in the last 24 hours for improper AI usage** (predominantly AI-generated submissions violating AGENTS.md):

- #28411 -- "ui : group the model selector components"
- #28410 -- "ui : discover download options"
- #28409 -- "ui : discover models browse view"

All three were part of a series authored primarily by an AI coding agent (pi, GLM-5.3-Flash) and closed by maintainers at the same time. A related draft PR (#28374) was also closed.
